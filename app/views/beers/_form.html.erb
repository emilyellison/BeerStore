<%= render 'shared/error_messages', item: @beer %>

<%= form_for @beer do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :price %>
	<%= f.number_field :price %>
	<%= f.submit 'Submit', class: 'btn' %>
<% end %>